import { Screen } from '@/types/app';
export default function Sidebar({open,setOpen,screen,setScreen}:{open:boolean;setOpen:(v:boolean)=>void;screen:Screen;setScreen:(s:Screen)=>void;}){const items:['generate'|'history'|'usage'|'logs'|'settings',string][]=[['generate','Generate'],['history','History'],['usage','Usage'],['logs','Logs'],['settings','Settings']];return <><button className='btn-secondary m-2' onClick={()=>setOpen(!open)}>☰</button>{open&&<aside className='fixed z-20 left-0 top-0 h-full w-56 bg-card border-r border-border p-4'>{items.map(([k,l])=><button key={k} className={`block w-full text-left p-2 rounded ${screen===k?'bg-blue-700':'hover:bg-slate-700'}`} onClick={()=>{setScreen(k);setOpen(false);}}>{l}</button>)}</aside>}</>;}
