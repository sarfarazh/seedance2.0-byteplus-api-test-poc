export type BytePlusTaskStatus = 'queued' | 'running' | 'succeeded' | 'failed' | 'cancelled' | 'expired';
export interface BytePlusTaskResponse { id:string; status:BytePlusTaskStatus|string; content?:{video_url?:string; last_frame_url?:string}; usage?:{total_tokens?:number; completion_tokens?:number; prompt_tokens?:number}; resolution?:string; ratio?:string; duration?:number; framespersecond?:number; created_at?:number|string; updated_at?:number|string; error?:{code?:string; message?:string}; }
